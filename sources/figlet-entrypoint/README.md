# Figlet Entrypoint

This repository contains our first Docker image example, which uses Alpine Linux as its base and installs Figlet with an
entry point to display a message customizable by the user.
