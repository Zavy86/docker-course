# Figlet (entrypoint and command)

This repository contains a Docker image example, which uses Alpine Linux as its base and installs Figlet with an entry 
point and a command to display an initial message customizable by the user.
